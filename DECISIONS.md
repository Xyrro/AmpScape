# Design decisions

Record every decision that refines or deviates from `docs/TASK_BRIEF.md`.

| Date | Decision | Rationale | Phase |
|------|----------|-----------|-------|
| 2026-09-05 | Project named AmpScape | fixed by brief | 0 |
| 2026-09-05 | Julia **1.11.3** via `module load julia/1.11.3` (not the 1.12.5 default) | Circuitscape 5.17.1 / Omniscape 0.6.2 resolve and precompile cleanly on 1.11; 1.10 is LTS but older; 1.12 is newest and least tested with this dependency stack | 0 |
| 2026-09-05 | Python via **uv** (CPython 3.11.16 managed by uv on scratch) instead of conda | reproducible `uv.lock`; complete isolation from the `kneeoa` conda env auto-activated by `~/.bashrc`; `torch` wheels bundle CUDA so no cluster CUDA module is needed | 0 |
| 2026-09-05 | GDAL CLI tools from a separate conda-forge env (`envs/gdal`, GDAL 3.9.3) appended to `PATH`; Python uses rasterio's bundled GDAL 3.10.3 | no GDAL module on PACE-ICE; keeps the Python env pip-only | 0 |
| 2026-09-05 | All caches/depots/envs live inside `$AMPSCAPE_SCRATCH` (= repo dir) and are git-ignored | 30 GB home quota; single scratch tree is easy to sync / purge-proof | 0 |
| 2026-09-05 | Keep the "no network in `sbatch` jobs" rule even though compute nodes were verified to have outbound HTTPS | robustness (policy could change; jobs must be replayable offline); exception: Julia precompile runs via `srun` because it needs no network but ~6 CPU-minutes | 0 |
| 2026-09-05 | **Reference solver = Circuitscape `solver = cholmod` (direct, double precision)**, not CG+AMG | the brief asks for relative residual ≤ 1e-8; in Circuitscape 5.17.1 the CG tolerance is hard-coded (`Krylov.cg rtol=1e-6`, accept if residual < 1e-4) and not exposed as an INI option; CHOLMOD is exact to round-off, deterministic, and fast for grids ≤ 1024². CG+AMG timings will still be recorded for the speed-up argument; XXL tier feasibility with CHOLMOD is measured in Phase 5 | 1 |
| 2026-09-05 | **Neighbour combination = average conductance** (`connect_using_avg_resistances = False`: g_ij = (g_i+g_j)/2, diagonal /√2). This **is the Circuitscape.jl 5.17.1 default** (verified in `src/raster/pairwise.jl::construct_graph`); the brief's phrase "Circuitscape default (average resistance)" mis-described the default, so this is a deviation from the *brief's wording*, not from the software. AmpScapeSolve.jl sets the key **explicitly** on every solve and it is recorded in each sample's `solver_params` | practitioners get the software default; explicit setting + recording protects against upstream default changes | 1 |
| 2026-09-05 | Circuitscape is driven through its public `compute(::Dict)` API with temporary ASCII/GeoTIFF files on node-local `/tmp`; Omniscape through the in-memory `run_omniscape(cfg, resistance, source)` method | Circuitscape 5.17.1 has no in-memory public API; writing to node-local disk is cheap and keeps us on the supported code path | 1 |
| 2026-09-05 | Omniscape `block_size` values in the tier table are odd (Omniscape bumps even values with a warning) | verified behaviour of Omniscape 0.6.2 | 1 |
| 2026-09-05 | Wall-to-wall T1 variant is stored as a separate task id `T1W` with 2 focal regions (N/S or E/W strips) | keeps T1 sample schema (K point nodes) uniform; strips are regions, not points | 1 |
| 2026-09-05 | Owner accepted CHOLMOD reference solver and average-conductance rule; brief §3.2 amended accordingly | owner decision | 1 |
| 2026-09-05 | Off-cluster sync = private HF dataset repo `Xirro/AmpScape`; shards validated → uploaded → sha256-verified → deleted locally; local working set < 150 GB | 300 GB scratch quota; owner decision | 1 |
| 2026-09-05 | Per-pair current/voltage maps stored only for K ≤ 4 | storage; owner decision (as in brief) | 1 |
| 2026-09-05 | Real-tile CRS = WGS84/UTM zone of tile centre (EPSG:326xx/327xx), EPSG recorded per tile | owner decision; UTM is near-equal-area within a tile and universally supported | 1 |
| 2026-09-05 | Roads from GRIP4 (CC BY 4.0, to be verified), OSM not used | ODbL share-alike avoided; owner decision | 1 |
| 2026-09-05 | Target venue NeurIPS 2027 D&B; Croissant export added to Phase 7 | 2026 deadline passed; Croissant mandatory | 1 |
| 2026-09-05 | HF org `Xirro`; `HF_ORG=Xirro` in CLAUDE.md | owner decision | 1 |
| 2026-09-05 | Synthetic cost fields map to resistance by R = contrast^f (log-uniform), not linear | spreads values evenly in log-resistance, which is what matters for current flow; linear mapping kept as an option (`mapping: linear`) | 2 |
| 2026-09-05 | Synthetic NoData: valid pixels reduced to the largest 8-connected component | guarantees a connected resistance graph so no focal node can be isolated by construction | 2 |
| 2026-09-05 | NLMpy calls wrapped in a seeded-global-RNG context (`seeded_global_rng`) | NLMpy 1.2.0 draws from `np.random` global state; wrapping keeps landscapes reproducible from `seed` alone | 2 |
| 2026-09-05 | Real covariate stack = 8 channels: landcover, elevation, slope, road_distance, road_class, river_distance, river_order, ghm (GeoTIFF, deflate, band descriptions) | minimal set that supports the four resistance tables of Phase 3; class one-hot is derived at load time | 2 |
| 2026-09-05 | Raster sources read as windowed `/vsicurl/` COG requests (WorldCover, Copernicus DEM) instead of downloading 3°/1° tiles | pilot moves ~50 MB instead of ~4 GB; keeps every extraction under the 5 GB gate | 2 |
| 2026-09-05 | Vector sources (GRIP4, HydroRIVERS) downloaded per region as shapefiles with `.qix` spatial indexes; pilot restricted to GRIP4 regions 1,2,3,5,7 (N/S America, Africa, Middle East & Central Asia, Oceania) | keeps the pilot download at 2.76 GB (< 5 GB gate); Europe (1.2 GB) and S/E Asia (0.7 GB) are added at full scale after owner approval | 2 |
| 2026-09-05 | gHM v1 at 1 km used for both stratification and the `ghm` channel (bilinear to tile grid) | only version with a verified stable download + CC BY 4.0; the 300 m temporal version is a later upgrade | 2 |
| 2026-09-05 | Strata = RESOLVE biome × realm × gHM tercile; tercile edges computed on the attributed candidate sample and recorded in `tile_specs.json` (pilot: 0.0329, 0.1821) | the brief's "continent" is approximated by RESOLVE realm; edges are stored so later samples use the same cut points | 2 |
| 2026-09-05 | Tile usability QC: reject if WorldCover NoData + water + snow/ice > 90 % or DEM NaN > 50 %; rejected tiles are replaced from a same-stratum reserve list | brief §4.2 rule plus a DEM-coverage guard | 2 |
| 2026-09-05 | Tile grid origin snapped to a whole pixel in UTM so `(lat, lon, size, pixel_m)` reproduces the grid exactly | reproducibility of tiles from the manifest alone | 2 |
| 2026-09-05 | HF storage decision deferred to the Phase 5 gate; until then plan only `ampscape-mini` + a small dev subset (< 100 GB total, fits the free private quota) | owner decision | 2 |
| 2026-09-05 | Download gate refined: ask before any **single download > 5 GB** or **cumulative source downloads > 20 GB**; GRIP4 regions 4 and 6 + HydroRIVERS as/si/ar/gr (~2.3 GB) approved | owner decision | 2 |
| 2026-09-05 | Roads: GRIP4 (PBL, Meijer et al. 2018) replaces the brief's OSM extracts. GRIP4 is PBL's own harmonised global roads database released under PBL's licence terms (CC0 per globio.info; catalogued as CC BY 4.0), not an OSM extract; we store only derived distance/class rasters | avoids ODbL share-alike; owner decision | 2 |
| 2026-09-05 | WDPA: not downloaded (account + terms; no redistribution, non-commercial for derived works). Phase 4 default = focal regions from land-cover habitat patches; WDPA-derived unlabelled masks only if UNEP-WCMC permission is obtained | licence incompatibility with CC BY 4.0 | 2 |
| 2026-09-05 | Resistance-table **numeric values are AmpScape's own** expert-style choices; only class ordering and term forms are taken from the cited literature (Cushman 2006, Koen 2014, Spear 2010, Zeller 2012, Brennan 2022) | published tables are species/region-specific and not reusable verbatim; Bowman et al. 2020 show current maps depend on rank order, not magnitudes | 3 |
| 2026-09-05 | Table combination order fixed: base (class / 1+a·gHM^b) → ×slope → ×elevation band → +road penalty (within `buffer_m` of a GRIP4 road, by class) → +gHM term → water rule → clip [1, r_max]; NoData = WorldCover class 0 (∪ table-specific classes) | one documented formula for all tables; recorded per sample via table sha256 | 3 |
| 2026-09-05 | `forest_bird` uses r_max = 100 (others 1000) | flying dispersers have a compressed cost range; also gives a lower-contrast table for the contrast axis | 3 |
| 2026-09-05 | `random_table` = log-normal (sd 0.5) perturbation of `large_mammal`, seed 20260905, materialised as `random_lm_20260905.yaml` (versioned) | reproducible, reviewable; candidate held-out table for `test_ood_table` | 3 |
| 2026-09-05 | Resistance rasters written next to the tiles as 2-band GeoTIFF (resistance, nodata) in `data/tiles/<set>/resistance/<table_id>/`; `resistance.parquet` holds per-raster stats + table provenance | keeps Phase 5 inputs file-based and inspectable; HDF5 packaging happens in Phase 6 | 3 |
| 2026-09-05 | Connectivity check uses an exact Python reconstruction of the Circuitscape graph (`sources/graph.py`: 8-neighbour, NoData removed, average conductance with diagonal /√2), unit-tested against the formulas in `pairwise.jl::construct_graph`; focal candidates are drawn from the largest component and every focal pixel is re-verified after placement | owner requirement 1; the same graph is reused for Phase 9 physics residuals | 4 |
| 2026-09-05 | Source parameters live in `configs/tasks/sources_default.yaml` (K ∈ [2,8], min separation 16 px, strip width 2 px, habitat classes {10,90,95}, min patch 50 px, max region 2000 px, T3 source = (1/R) above the 0.7 quantile normalised to Σ = 1, grounds ∈ {edge, all_edges, patches}, T4 source = (1/R) above the 0.5 quantile scaled to max 1, source_threshold 0); pixel quantities scale with tier (×2 M, ×4 L, ×8 XL, ×16 XXL; areas ×scale²) | owner requirement 2 | 4 |
| 2026-09-05 | Point placement is per node: with probability `frac_anywhere` = 0.3 a node may be any pixel of the largest component, otherwise it must have R ≤ the 0.25 quantile of valid R; each node's placement is stored in the focal table and the sample summary (`anywhere` / `low_resistance` / `mixed`) | owner requirement 3; avoids focal nodes systematically sitting in barriers while keeping some hard cases | 4 |
| 2026-09-05 | Focal nodes stored as int32 label raster **and** a table (label, row, col, kind, placement, n_pixels, resistance; regions/strips add a pixel-coordinate polygon WKT) | brief §6 / owner requirement 4 | 4 |
| 2026-09-05 | Multi-pixel focal regions are passed to Circuitscape as repeated labels in the point raster (verified with `examples/region_check.jl`: repeated labels are short-circuited, Reff 0.47 vs 1.04 for single pixels; focal-region pixels carry the full injected current, so they are excluded from map metrics) | settles the Phase 1 open item; no polygon file needed | 4 |
| 2026-09-05 | Focal regions come from WorldCover habitat patches (tree / wetland / mangrove), not WDPA; only 23 / 60 pilot tiles have ≥ 2 eligible separated patches, so region samples are an *additional* configuration on tiles that support it, never a replacement for point samples | WDPA licence; owner decision Phase 3 | 4 |
| 2026-09-05 | Synthetic-landscape source configurations are generated with the same code and config (50-landscape sanity set in `sources.parquet`, family = synthetic) | one code path for both families | 4 |
| 2026-09-05 | Phase 5 QC will flag `frac_at_rmax > 0.5` samples, exclude them from train/val, and keep them in the Parquet index with the flag set (never deleted) | owner decision on the forest_bird saturation | 4 |
| 2026-09-05 | **Omniscape reference solver = CHOLMOD too** (`configs/solver/omniscape_reference.yaml`) | with cg+amg, Omniscape re-throws any window whose CG residual exceeds Circuitscape's hard-coded 1e-4 and the whole T4 solve fails; observed on a contrast-10⁴ synthetic landscape in the dev shard. CHOLMOD solved it (9.7 s at 128², r=16, b=3) | 5 |
| 2026-09-05 | Kirchhoff residual is computed **in Julia from the full-precision voltages** before the float32 cast and stored in `solver_params.residual_rel` (QC threshold 1e-8); a second residual from the stored float32 maps (`residual_rel_f32`, threshold 5e-2) only guards against layout errors | float32 storage alone yields 1e-4–1e-2 relative residual; CHOLMOD gives ~1e-11 | 5 |
| 2026-09-05 | Pair convention recorded in every sample (`pair_convention`): for (i, j) node i grounded, 1 A injected at j (Circuitscape behaviour, verified) | needed for physics metrics and for reading the voltage maps | 5 |
| 2026-09-05 | Residual for short-circuited focal regions uses the supernode form (region rows replaced by Σ_region L v = Σ_region b) in both Julia and Python | Circuitscape treats repeated labels as one node; row-wise checks are meaningless there | 5 |
| 2026-09-05 | Circuitscape is driven through temp ASCII grids on node-local `$TMPDIR` inside the job; Omniscape in memory; one shard = one Julia process = one Slurm array task, `JULIA_NUM_THREADS=1`, CHOLMOD/BLAS get the allocated cores | Circuitscape 5.17.1 has no in-memory API; per-process solves keep `maxrss` interpretable and avoid thread-safety questions in Circuitscape's global logging | 5 |
| 2026-09-05 | Each Slurm job pays ~3.5 min Julia load/JIT before solving; shards should hold ≥ 50 tier-S samples (≥ 10 min of solves) so overhead stays < 25 % | measured on the 5-sample smoke build (job 4:47 wall, 78 s solving) | 5 |
| 2026-09-05 | QC flag semantics: `not_converged`, `nonfinite_output`, `all_zero_output`, `residual_high`, `residual_f32_high`, `conservation_high`, `isolated_focal`, `omniscape_edge_artifact` → `qc_pass = False` (excluded everywhere, kept in index); `rmax_saturated` → excluded from train/val only; `fallback_solver` informational | owner rules (Phase 3/4) + brief §7.2 | 5 |
| 2026-09-05 | Mini-run sampling: 200 synthetic landscapes = seeds 20260905…+199 drawn from the documented prior (all 7 generators appear: fractal 54, grf 51, mosaic 29, distance 22, cluster 22, planar 13, edge 9); 50 real = 10 tiles per table, one table per tile, regions config added where available; every landscape gets points (K∈[2,8]), NS + EW strips, T3, T4 | covers all generators, tables and source configs with a single seed | 5 |
| 2026-09-05 | `HF_ORG = Xirro` confirmed by the owner (an earlier message contained an unfilled placeholder) | owner decision | 5 |
| 2026-09-05 | QC residual threshold **1e-6** (not the brief's 1e-8) and the float32-recomputed residual is informational, not a flag | mini run: CHOLMOD residuals are 1e-12–1e-9 except contrast-10⁴ advanced systems at 2.3e-8 (double-precision floor ≈ eps·‖L‖‖v‖/‖b‖ ≈ 1e-8); CG+AMG sits at 1e-6–3e-5, so 1e-6 still separates the solvers; float32 rounding of |v|~10³ voltages gives O(0.1–1) recomputed residuals that say nothing about the solve | 5 |
| 2026-09-05 | Omniscape window geometry per tier: S r16/b3, M r32/b5, L r64/b9 unchanged (measured 6 s / 48 s / 178 s median per solve, all < 10 min); **XL block 17 → 33** (measured 13 min median, 21 min max at b17; ≈ 3.8× fewer windows brings it under 10 min); XXL r256/b65 (b33 measured: 4 468 s = 74 min, 2.4 GB; b65 ≈ 19 min estimated) | owner requirement: each Omniscape solve < 10 min for S/M/L; keep XL/XXL affordable | 5 |
| 2026-09-05 | T4 (Omniscape) is the cost driver: ~90 % of per-sample solve time at tier S (6.2 s of 6.6 s) and > 97 % at L/XL. Budget ladders are therefore quoted with T4 on every landscape; solving T4 on a subset is listed as the main lever for the owner | measured in the mini run and probes | 5 |
| 2026-09-05 | `generate.py submit` precompiles the Julia package on the login node before `sbatch` (skippable with `--skip-precompile`) | array tasks started with a stale compiled cache raced on the shared depot's precompile pidfile and hung for > 20 min (observed on the M/L/XL re-solve arrays); CLAUDE.md already requires precompilation on the login node | 5 |
| 2026-09-05 | **Framing change:** the dataset is designed as a complete standard benchmark, not fitted to ICE's budget; ICE hosts the pipeline, mini and dev shards only; the v1.0 run location (likely a University of Minnesota cluster) is chosen later from the cost table in `docs/dataset_plan.md` | owner decision | 5 |
| 2026-09-05 | Owner accepted: CHOLMOD reference for all tasks with CG+AMG fallback only; QC residual threshold 1e-6; **T4 on every landscape** (no subset) | owner decision | 5 |
| 2026-09-05 | Omniscape block size is chosen on **fidelity**, not cost: b17 vs b33 at XL and b33 vs b65 at XXL are quantified on 3 samples each (relative L2, max difference, correlation of `cum_current`); the cheaper setting is adopted only if relative L2 < 1 % | owner decision; results in `docs/dataset_plan.md` | 5 |
| 2026-09-05 | HF storage plan deferred until the run location is settled | owner decision | 5 |
| 2026-09-05 | v1.0 design (`configs/datasets/v1_0.yaml`, `docs/dataset_plan.md`): recommended ladder S 100k / M 50k / **L 20k / XL 4k / XXL 400** (brief: 10k / 2k / 200) | scale-OOD statistics: XL/XXL are test-only and L is the largest training tier; the brief's counts leave < 30 landscapes per generator × table cell at XL/XXL | 5 |
| 2026-09-05 | Named `hard` stratum = 20 % of synthetic landscapes at every tier: contrast 10⁴ (30 %), r_max-saturated (15 %), narrow corridors (30 %, new generator in Phase 6), large NoData 0.25–0.45 (25 %) | owner requirement; makes hard cases countable instead of an accident of the prior | 5 |
| 2026-09-05 | One seeded random resistance table per real tile (seed = tile hash), `forest_bird` held out as `test_ood_table`; OOD region = biomes Montane Grasslands & Shrublands + Mangroves and realm Australasia; XL + XXL = `test_ood_scale`; contrast 10⁴ = `test_ood_contrast` | decorrelation per tile; forest_bird is the structurally distinct table (r_max 100, elevation bands) | 5 |
| 2026-09-05 | Cluster profiles (`configs/cluster/*.yaml`) hold every site-specific value; `generate.py submit --profile` and `scripts/env.sh` read them; `docs/run_guide.md` documents a fresh Slurm deployment | portability requirement; v1.0 will run elsewhere | 5 |
| 2026-09-05 | Cost accounting in `docs/dataset_plan.md` is in single-threaded core-hours (+15 % overhead) because every solve is single-threaded; memory, not cores, sizes XL/XXL allocations | measured on ICE | 5 |
| 2026-09-05 | **Omniscape block rule = largest odd integer ≤ radius/10** (b/r ≤ 0.10: S 1, M 3, L 5, XL 11, XXL 25). Fidelity study on 3 samples per tier: coarsening XL 17→33 = 2.5–2.9 % rel. L2, XXL 33→65 = 5.9–11.9 % (both rejected, > 1 %); anchors vs exact block 1: S b3 = 4.5 %, M b5 = 2.0 %. ≈ 3.3× the Phase-5 T4 cost; Phase-5 blocks kept as `phase5` option; mini was generated with the Phase-5 blocks | owner: choose on fidelity, adopt cheaper only if < 1 % | 5 |
| 2026-09-05 | Superseded note: an interim radius/8 recommendation was written while the block-1 anchor results were pending; the adopted rule is b/r ≤ 0.10 (row above). Both options are priced in `docs/dataset_plan.md` §6.1/§6.2 | consistency | 5 |
| 2026-09-05 | `docs/dataset_plan.md` recommends L 20k, XL 4k, XXL 400 (brief: 10k / 2k / 200), a named hard-case stratum (20 % of synthetic), one seeded random table per tile, and a 150-tile-per-biome floor; total 174 400 landscapes / 902 904 solves, ≈ 11 300 CPU-h with the fidelity blocks (≈ 3 980 CPU-h with the Phase-5 blocks), ≈ 810 GB | compute-agnostic framing; statistics per OOD cell | 5 |
| 2026-09-06 | **Project renamed EcoFlowBench → AmpScape** everywhere: Python package `ampscape`, Julia package `AmpScapeSolve.jl` (UUID unchanged), HF dataset `Xirro/AmpScape`, CLI `ampscape`, env-var prefix `EFB_` → `AMPSCAPE_` (all variables, not only `AMPSCAPE_SCRATCH`, for consistency), job-name prefix `ampscape-`; the scratch directory path (`…/yxiao413/EcoFlowBench`) and the git history are unchanged; git remote → `git@github.com:Xyrro/AmpScape.git` | owner decision | 6 |
| 2026-09-06 | Consistency sweep after the context loss: the only pre-adoption block-rule wording left in tracked files was the Phase 5 report's cost-driven XL 33 / XXL 65 proposal (historical); a superseded banner was added to the report generator; the `phase5` option block in `configs/datasets/v1_0.yaml` is intentional (priced alternative) | owner request B | 6 |
| 2026-09-06 | Plan approved by the owner at the recommended ladder with the radius/10 block rule and all six deviations | owner decision | 6 |
| 2026-09-06 | **C1 spatial splits shared across tiers:** 5° lon/lat blocks, one seeded assignment per block applied at every tier, footprint rule (test-most assignment wins), OOD regions = whole blocks; `ampscape/splits` + a cross-tier overlap test | owner amendment; multi-resolution tiles overlap across tiers | 6 |
| 2026-09-06 | **C2 resampling rules per layer:** majority (classes), mean (elevation, gHM), min (distances via rasterise-then-EDT), nearest feature (road class / river order); recorded in `meta.resampling` per sample; M–XXL area resampling to be implemented in `real.py` | owner amendment | 6 |
| 2026-09-06 | **C3 XL split:** 25 % of XL landscapes train/val (by block), 75 % test; `test_ood_scale` = XL/XXL test for models trained on ≤ L | owner amendment | 6 |
| 2026-09-06 | **C4 download subsets:** mini ≈ 0.5 GB (S), core ≈ 50 GB (S–L, all tasks/families/OOD sets), full; nested fixed splits; HF layout by tier/task group so any tier or task downloads alone | owner amendment (PGLearn-style) | 6 |
| 2026-09-06 | **C5 T1R on synthetic patch-mosaic landscapes** (lowest-cost class patches ≥ min_patch_px) in addition to real tiles | owner amendment | 6 |
| 2026-09-06 | **D mini regenerated under the radius/10 rule** (S block 1, dataset version 0.2.0-mini); the Phase-5 mini kept as `data/builds/mini_phase5blocks` (SUPERSEDED.txt) | owner decision; Phases 6–9 build on final-fidelity data | 6 |
| 2026-09-06 | **E streaming upload:** shards are validated → uploaded to HF → checksum-verified → deleted locally as they complete (no accumulation on scratch); storage (≈ 810 GB) is the binding constraint; design in Phase 6 (`ampscape/io/sync.py`), pushes gated by the owner | owner decision | 6 |
| 2026-09-06 | C1 refinement: assignment unit = **20° macro-cell** (not 5° blocks), straddling tiles excluded by the sampler instead of cascading; verified 0 cross-tier overlaps and ≈ 78/8/10 proportions on a 1 640-tile synthetic test; the 5° variant made most of the globe test | the footprint cascade with 5° blocks inverted the split proportions (L: 317 test vs 71 train) | 6 |
| 2026-09-06 | Omniscape's `project_name` directory (created in the cwd even with `write_outputs=false`, suffixed `_N`) is now a per-solve temp dir removed after the solve; per-job working dirs live in node-local `$TMPDIR` (cleaned on exit) or `$AMPSCAPE_SCRATCH/cache/<job>`; `cache/`, `efb_*/`, `ampscape_*/` gitignored | 505 empty `efb_N`/`ampscape_N` directories had accumulated in the repo root (owner finding) | 6 |\n| 2026-09-06 | C1 revised (owner check): assignment regions = **equal-width 20° grid (104 cells) + XXL footprints as their own regions** with inheritance for contained tiles and geometric overlap testing; plain 20° lon/lat cells were 1 570 km wide at 45° (XXL unplaceable: 0 % of random centres fit even at the equator), equal-width 30° bands confined XXL to |lat| 9–21°/39–51°/69–81° | multi-resolution tiles must be placeable at every latitude with zero cross-tier overlap | 6 |\n| 2026-09-06 | **HF layout** `data/<tier>/<task_group>/shard-NNNNN.h5` with task groups T1 (points: T1+T2), T1W, T1R, T3, T4; inputs duplicated into every task-group shard (≈ 0.1 MB per S sample) so any tier or task group downloads alone; `index/<tier>.parquet`, `splits/<subset>/<split>.parquet`, `stats/`, `croissant.json`, `README.md` | owner requirement (PGLearn-style); duplication cost ≈ 30 % at S, < 5 % at L+ | 7 |
| 2026-09-06 | Loader `AmpScapeDataset(task, split, tier, root)` reads either a finalized build or the HF layout; `load_from_hub` uses `snapshot_download` with `allow_patterns` per tier/task group instead of a `datasets` loading script (deprecated on the Hub for custom formats) | HDF5 shards are not a `datasets`-native format; pattern download achieves "download alone" | 7 |
| 2026-09-06 | Subsets mini/core/full assigned by per-sample hash, stratified by tier × family × split, nested; the mini release is the whole 250-sample mini build | owner amendment C4 | 7 |
| 2026-09-06 | Croissant 1.0 with RAI fields generated by `scripts/export_croissant.py`, validated with `mlcroissant` (0 errors, 0 warnings) | NeurIPS D&B requirement | 7 |
| 2026-09-06 | Dataset card states the honest framing: solver = ground truth; real landscapes with synthesized, literature-informed but AmpScape-valued resistance; Omniscape block approximation quantified; pilot skew of the mini's real tiles | owner requirement | 7 |
| 2026-09-06 | **Contrast ladder extended to {10, 10², 10³, 10⁴, 10⁵, 10⁶}** (external review): training includes up to 10⁵; 10⁶ is reserved for `test_ood_contrast`; the synthetic prior draws 10⁶ at half weight; hard-case stratum split into `high_contrast_1e5` (20 %) and `high_contrast_1e6` (10 %, test-only). Residuals at 10⁵/10⁶ measured on 5 S + 5 XXL samples each before adoption (report below) | review item 1 | 7 |\n| 2026-09-06 | Current maps are transformed as **log10(C + ε·max C), ε = 1e-6** for loader normalisation and metrics (replaces log1p, which is scale-dependent); SSIM/PSNR demoted to secondary | external review item 3 | 7 |
| 2026-09-06 | **Solver-acceleration evaluation track** (Phase 9): CG iterations/time to the reference residual from the predicted voltage vs zero on test_id + OOD splits for T1/T3; zero-start baseline recorded per sample in `SolveStats.cg_baseline`; documented as a supported use case in the task spec | external review item 2 | 7 |
| 2026-09-06 | Brief §17 and the datasheet state that domain-metric acceptance thresholds are unvalidated; results are reported without claiming sufficiency | external review item 4 | 7 |
| 2026-09-06 | `cg_baseline` is an explicit AMG-preconditioned CG (AlgebraicMultigrid `ruge_stuben`, `ldiv!`) rather than `Krylov.cg` with the AMG object as `M`: Krylov applies `M` with `mul!` semantics, which mis-applied the preconditioner (109 iterations on a 10×10 toy where PCG needs 8; stalled at 128²). One PCG run records iterations/time to every target residual; the Krylov dependency was removed from the package | correctness of the acceleration baseline | 7 |
| 2026-09-06 | Survey of downloadable published resistance surfaces done (`docs/survey_resistance_surfaces.md`, nothing downloaded): three permissive candidates with explicit resistance rasters (Eurac Alps CC BY 4.0; raccoon Europe CC BY 4.0; Hawaiian gallinule CC0) plus CC0 USGS fire-connectivity outputs; recommended as a small "published-resistance" OOD evaluation set pending owner approval | external review item 5 | 7 |
| 2026-09-06 | Config lists passed to array jobs are `+`-separated in `--export` (sbatch splits `--export` on commas): a `points,advanced` re-solve silently ran `points` only | bug found when the T3 acceleration baselines were missing | 7 |\n| 2026-09-06 | `SolveStats.cg_baseline` is recorded only for test/OOD samples (all tiers): the provisional split is assigned at plan time (`assign_plan_splits`, same rules as the final split) and written as a per-sample `cg_baseline` attribute; XXL memory: 9.5 GB train/val shards, ≈ 14.5 GB test shards (`XXL_test` profile entry, chosen automatically by `submit`) | owner decision | 7 |\n| 2026-09-06 | **Solver cross-check re-run (owner item 3)** on 10 mini samples (K ≤ 4 first pair + T3): AmpScape AMG-PCG vs CHOLMOD — max relative difference 1.9e-9 (pair current), 2.1e-11 (Reff); PCG residual ≤ 8.5e-11 (9–25 iterations to 1e-6, ≤ 33 to 1e-10, ≤ 0.13 s); Circuitscape's own cg+amg vs CHOLMOD 6.0e-7 (cum current), 3.4e-9 (Reff), residual ≤ 1.6e-6 (T1) / 4.0e-5 (T3). **Confirmed: every stored output in every build (mini, mini_phase5blocks, smoke5, all probes; 2 660 configurations) has `solver = cholmod` and `fallback_used = false`; no stored output ever came from a CG path.** The Phase 5 comparison used Circuitscape's cg+amg, which was never broken; only the later-added AMG-PCG acceleration baseline was mis-preconditioned (fixed 2026-09-06). Banner added to the Phase 5 report | owner item 3 | 7 |
| 2026-09-06 | Published resistance rasters downloaded (owner item 4): Eurac Alps landscape permeability (CC BY 4.0), raccoon Europe output maps (CC BY 4.0), Hawaiian gallinule resistance layers (CC0); sha256 in `data/sources/manifest.json`; a `test_ood_published` split (their resistance as given, AmpScape focal/source configs, all applicable tasks) is a Phase 9 deliverable | owner item 4 | 7 |
| 2026-09-06 | **Publication route (owner item 5):** the mini was pushed to `Xirro/AmpScape` (private, free tier) as the end-to-end test of the sync path; v1.0 is generated and assembled on a cluster with ≥ 1 TB scratch and published to HF as a public dataset in one push; the streaming validate→upload→verify→delete sync remains a fallback | owner decision | 7 |
| 2026-09-07 | Dryad blocks scripted downloads from the cluster (401 API / 403 web stream); the Hawaiian gallinule archive is left to a manual browser download by the owner; the other two published rasters are in the manifest (cumulative sources 5.15 GB, under the 20 GB gate) | download gate bookkeeping | 7 |\n| 2026-09-07 | **Contrast-ladder probe resolved (owner item 1).** (a) The residual for short-circuited focal regions is now that of the *collapsed* system Circuitscape solves (region rows summed, ‖b‖ = total injected current) in both Julia and Python; the earlier "1 of 5 fail" at XXL was an artefact of normalising by the per-pixel injection (1/√2048), which inflated the value 45×. (b) **One step of CHOLMOD iterative refinement** (`refine_voltage!`: our own SuiteSparse factorisation of the reduced/collapsed system, r = b − Ax, A d = r, x += d, then per-pair current maps and Reff regenerated with `node_current_map`, validated against Circuitscape's maps) runs whenever the achieved residual exceeds 1e-8; recorded per sample in `solver_params.refinement`. (c) Measured (XXL wall-to-wall, 5 samples per contrast): 10⁵ max 4.0e-9, no refinement needed; 10⁶ max 2.75e-8 → refined to 6.6e-9 (+16.7 s on a 60 s solve), others ≤ 1.3e-9. All point-source and advanced solves at S/XXL, 10⁵/10⁶ ≤ 2.3e-7. **Uniform QC threshold 1e-6 kept.** Refinement cannot reuse Circuitscape's internal factorisation (not exposed), so the trigger costs one extra factorisation only when it fires | owner decision 1 | 7 |
| 2026-09-07 | Contrast ladder {10 … 10⁶} confirmed: every CHOLMOD residual at 10⁵/10⁶ (S and XXL, T1/T1W/T3) is below the 1e-6 QC threshold with the consistent definition; `test_ood_contrast` = 10⁶ | external review item 1, closed | 7 |
| 2026-09-07 | Phase 8: `docs/licenses.md` reconciled with all 21 manifest entries (5.45 GB) incl. the three published rasters; verification log added; `CITATION.cff` completed with upstream references; redistribution-terms section in the dataset card; `tests/test_licenses.py` enforces licence + sha256 + documentation per manifest entry | brief §10 | 8 |
| 2026-09-07 | Unverifiable / weakly verified items listed for the owner: GRIP4 licence statement (CC0 on globio.info vs CC BY 4.0 in catalogues vs one ODbL listing) — cannot be resolved without PBL; WorldCover, HydroRIVERS and RESOLVE licences were read from site text as surfaced by search and should get one manual check before publication; everything else verified from repository APIs or the licence document | honesty about provenance | 8 |
| 2026-09-13 | Phase 9 metrics: pixel metrics in log10(C + ε·max C_target) space; domain metrics with documented conventions (top-q sets exclude zero-flow pixels; pinch points = 7×7 local maxima in the top 5 %, hit radius 3 px; corridor Dice at q = 10 %); Reff metrics incl. symmetry; physics checks (Kirchhoff residual on the exact graph when a voltage is predicted, per-pair focal-current check, non-negativity, throughput proxy); speed-up; SSIM/PSNR secondary; T1W strip pixels masked | brief §11 + owner spec 1; every metric unit-tested on hand cases and anchored by an oracle (perfect scores) and an all-zero predictor | 9 |
| 2026-09-13 | Solver-acceleration metric runs in Julia (`scripts/warm_start_eval.jl`) with the same AMG-PCG, matrix and tolerance as the stored zero-start baselines, warm-started from the predicted voltage map | identical solver on both sides; Python has no AMG dependency | 9 |
| 2026-09-13 | Predictions format = one HDF5 file (`/<sample_id>/<config>/…`, attr `inference_time_s`) + `meta.json`; `evaluate.py` is deterministic (sorted samples, no randomness) and evaluates any subset present in the file | owner spec 2; `docs/evaluation.md` | 9 |
| 2026-09-13 | `test_ood_published`: published resistance used as given, resampled by the **geometric mean of R** (area mean of log R) to the nearest tier with pixel size ≥ native (Eurac 20 m → S, gallinule 10 m → S, raccoon 1 km → XXL at native resolution, no upsampling); NoData cap 10 % at S, 45 % at XXL (sea); tiles at 128 px: Eurac 30, gallinule 15, raccoon 1 XXL tile (eastern Baltic, the only 2 048 km placement with < 45 % sea); provenance (DOI, licence, CRS, native resolution, rule, rescale factor) in every tile and sample | owner spec 3 | 9 |
| 2026-09-13 | Non-learned baseline: coarsen ×4 (geometric-mean resistance, majority NoData, any-label focal, sum/mean sources), reference CHOLMOD solve on the coarse grid, bilinear upsampling of maps, Reff from the coarse solve. Current maps of pairwise/advanced configs are divided by f (a coarse node collects the flow crossing f fine pixels; Omniscape maps are left unscaled because their sources were mean-pooled), coarse focal pixels are in-filled from the nearest non-focal coarse pixel before upsampling and the true focal pixels reset to 1 A (without this the naive baseline stamped f-wide bands with the injected current: T1W rel-L2 1.91 → 0.24). T3: a coarse block holding both source and ground becomes ground only (Circuitscape leaves a node that is source and ground ungrounded — 27 of 32 coarse ground nodes of one mini sample sat at up to 4.7 V), the remaining sources are renormalised, and the block-summed injection is removed before the 1/f scaling and the fine injection added back | brief §12.1 / owner spec 4; scale checked on the mini | 9 |
| 2026-09-14 | **v1.0 planner (`ampscape/solve/plan_v1.py`)**: per-tier deterministic streams so any first-n subset (dev, core) is a true prefix of v1.0 — synthetic seeds `TIER_SEED_BASE[tier] + i` (S 1e8, M 2e8, L 3e8, XL 4e8, XXL 5e8; tier-disjoint), landscapes from `sample_landscape_v1`; real = accepted tiles of the v1.0 tile stream in extraction order × 5 tables; source seed = sha1(tile|table); sample ids UUID5 under `ampscape-v1.0` | owner request: dev subset = prefix of v1.0, reusable while the pipeline is frozen | dev |
| 2026-09-14 | v1.0 synthetic sampler (`sample_landscape_v1`): base generators from the plan mix (renormalised over 80 %), contrast uniform on {10, 10², 10³, 10⁴}; with p = 0.20 a hard case by the plan shares — `high_contrast_1e5`, `high_contrast_1e6` (test-only via the contrast hold-out), `rmax_saturated` (55–75 % of valid pixels set to r_max), `narrow_corridor` (1–3 walls of 2 px spanning the raster, orientation within ±15° of an axis, 1–3 gaps of 1–3 px, contrast ∈ {10², 10³, 10⁴}), `large_nodata` (NoData fraction 0.25–0.45). `params.hard_case` / `design` recorded and indexed. The Phase-2 sampler is untouched bitwise (fingerprint test) | dataset plan §3 (gap items closed) | dev |
| 2026-09-14 | T1R on synthetic patch-mosaic landscapes (amendment C5): habitat = the lowest-cost class (cost 0 → R = 1) passed to `sample_regions` as class 10; `regions` planned on every mosaic landscape and on every real landscape, prepare skips landscapes without eligible patches | C5 | dev |
| 2026-09-14 | v1.0 real-tile stream (`scripts/build_v1_tiles.py`): the full v1.0 selected lists (S 8 000, M 4 000, XXL 32, seeds 20260906·10 + tier index) are sampled once with the stratified round-robin sampler; S–XL candidates must fit one split macro-cell — straddling candidates are snapped to the cell's interior box and re-attributed (0.8–1.8 % of candidates), XXL tiles are their own assignment regions and need no fit; extraction is prefix mode (`extract_tiles.py --first-accepted`, rejected tiles skipped, never replaced); one seeded `random_lm` table per tile (seed sha1(tile)); the 32 XXL centres are frozen as provisional assignment regions (`parents.parquet`) so S–XL splits are identical whether or not the XXL tiles are built | dataset plan §2.1/§5 | dev |
| 2026-09-14 | XL 25 % train/val share used Python's salted `hash()` (non-deterministic across processes) — replaced by a sha1-based unit (`stable_unit`); no XL data existed, nothing to regenerate | reproducibility bug | dev |
| 2026-09-14 | `test_ood_region` hold-out unit made configurable (`unit: tile` default, `cell` = previous behaviour): on the dev prefix the cell rule turned 52 % of real S tiles into ood_region (mangrove/montane tiles touch 23 of 56 cells) and left no real test_id tile; tile-level = a tile is OOD when its own biome/realm is held out (24 % of dev tiles) | dev subset finding; **owner decision pending** (plan §4 wording said whole cells) | dev |
| 2026-09-14 | XXL parent assignment regions disabled (`spatial_block.parent_regions: false`): with the real v1.0 XXL centres the 32 footprints merge into 13 continental components covering 72 % of the S tiles, and five held-out parents propagate ood_region to 12 of 13 components (1 real S test_id tile). S–XL tiles are assigned by macro-cell; XXL stays test-only; S-inside-XXL overlap ≤ 0.004 % of an XXL tile | dev subset finding; **owner decision pending** (amendment C1); split labels are recomputed at finalize, samples unchanged | dev |
| 2026-09-14 | Wall-to-wall and advanced configurations are skipped (not solved) when undefined on a landscape — an edge strip with no valid pixel, or a ground mode with no ground / no source on the largest component (advanced first falls back to the other ground modes) — instead of aborting the shard; affects large-NoData hard cases (25 of 95 in dev S lack one wall-to-wall orientation) and coastal tiles | robustness | dev |
| 2026-09-14 | PyTorch from the cu126 wheel index (`[tool.uv.sources]`): ICE GPU nodes run driver 575 / CUDA 12.9 and the PyPI torch 2.14 wheel (CUDA 13.0) refuses to initialise; all four baselines verified on an L40S with bf16 autocast (FNO runs in fp32: complex spectral weights) | environment | 10 |
| 2026-09-14 | **Owner (Phase 10-dev accepted):** `test_ood_region` hold-out at tile level is the default | confirmed | 10 |
| 2026-09-14 | **Owner:** XXL parent regions stay disabled; XXL is test-only; for XXL `test_ood_scale` isolates resolution and spatial overlap with finer-tier training regions is unavoidable and disclosed (datasheet, task spec); XL remains overlap-free via cell assignment. New subset `test_ood_scale_strict`: ≈ 6 XXL real tiles placed entirely inside held-out (non-train) cells, zero overlap with any training tile at any tier, generated with v1.0 | confirmed | 10 |
| 2026-09-14 | **Owner:** tuning pass on dev (≤ 6 GPU-h): FNO 32/64 modes, multi-scale / coarsened-graph GNN, one alternative each for U-Net and ViT, a distance-to-source input channel offered to every model; resulting configs frozen as the official baseline configs | tuning | 10 |
| 2026-09-14 | **Owner:** external GPU request sized at 1 000 GPU-hours (`docs/tables/gpu_budget.md`) | sizing | 10 |
| 2026-09-14 | **Owner — generation decision:** v1.0 is generated on ICE with streaming upload to a **public** `Xirro/AmpScape`; the 500 CPU-hour gate is lifted for this run (budget 13 000 core-hours incl. contingency); the 20 GPU-hour gate stays. Supersedes the assemble-then-publish route (2026-09-06). Freeze checklist (tag `v1.0-pipeline`, reproducibility of two dev shards, full real-tile set, public flip with in-progress notice, live streaming sync with 200 GB scratch guard, runbook) precedes the launch; tier S launches first after checklist approval | owner | v1.0 |
| 2026-09-14 | **Frozen macro-cell assignment** (`configs/splits/cell_assignment_v1.json`, `scripts/freeze_cell_assignment.py`): every land cell of the 20° equal-width grid (70 of 104) gets one seeded train/val/test_id label (56/7/7), stratified by the cell's dominant RESOLVE realm from the v1.0 S candidate points; `assign_tiles` uses it whenever the seed matches. Before, `assign_blocks` cut the hash-sorted cells *present in the tile set*, so a cell's label depended on which tiers/subsets had tiles in it (25 S and 73 M dev labels moved) | pure function of (cell, seed) as the plan §5 promised; v1.0 freeze | v1.0 |
| 2026-09-14 | Multi-tier tile reader: when the target pixel is ≥ 2× the source pixel the COG window is read decimated (`ds = floor(ratio/2)`; mode for land cover, average for continuous) from the overviews and then reprojected (amendment C2; per-channel provenance in every tile and sample `resampling`). Consequence: S land cover is now the 5×5 majority (was nearest), so **real dev tiles are not reproduced bitwise and the dev subset's real half is regenerated as part of v1.0** (owner's stated fallback); the synthetic half is unchanged | XXL tiles were infeasible with full-resolution reads (40 Gpx); C2 said majority | v1.0 |
| 2026-09-14 | Tile QC: DEM gaps are counted over land only (`frac_dem_nan_land` ≤ 10 %); ocean has no Copernicus DEM cells and coastal XXL tiles were rejected for water that the land-cover water fraction already accounts for | XXL timing test (66 % water tile rejected on DEM) | v1.0 |
| 2026-09-14 | v1.0 tile set lives in `data/tiles/v1.0` (fresh prefix extraction of the same frozen lists, per-tier manifests merged by `build_v1_tiles.py merge`); `data/tiles/v1` stays as the dev-era set | concurrent per-tier extraction; provenance separation | v1.0 |
| 2026-09-14 | Streaming sync live mode (`sync_shards.py --live`): validate → split by task group → upload → verify Hub sha256 → `.uploaded` → delete final + staged files; a shard failing verification twice gets `.upload_failed` (stop rule); `--publish-index` re-publishes `index/<tier>.parquet` + split lists; `generate.py submit` refuses above 200 GB under `data/` (`limits.scratch_pause_gb`) | owner checklist e | v1.0 |
| 2026-09-14 | `Xirro/AmpScape` flipped to **public** with the in-progress notice; the 39 dev-era files (mini shards, index, splits, stats, croissant) removed first | owner checklist d | v1.0 |
| 2026-09-14 | Pipeline frozen at tag **`v1.0-pipeline` = `8491bbe`**; recorded as `pipeline_tag` + `pipeline_git_sha` in every shard root, sample meta and index row | owner checklist a | v1.0 |
| 2026-09-15 | Reproducibility (checklist b): synthetic and real dev shards regenerated from `v1.0-pipeline` are bitwise identical when solved on the same CPU model (3 of 4 shards); across ICE node types CHOLMOD/BLAS results differ in the last bits (≤ 1.2e-9 relative, S shard 18). The dev subset is therefore regenerated as part of v1.0 (also required by the C2 land-cover change); reproducibility is documented as bitwise on identical hardware, ≤ 1e-9 relative across CPU types | owner rule; datasheet | v1.0 |
| 2026-09-15 | v1.0 tile set complete (`data/tiles/v1.0`): S 8 000 / M 4 000 / L 1 600 / XL 320 / XXL 32 + 6 strict accepted, 69 790 resistance rasters; lists extended by 5 % (prefix verified identical) to absorb rejections; strata short of the plan floor: Mangroves 128 S tiles (OOD-only biome) and RESOLVE 'N/A' 28 | checklist c | v1.0 |
| 2026-09-16 | **Shard integrity (owner):** the solver writes a completion marker `<shard>.outputs.h5.done` last (after closing both files, only when every input sample has a complete outputs group and the run was not partial); finalize refuses shards without it (`--legacy-no-marker` only for the 91 pre-marker tier-S tasks, with the completeness guards); prepare records `planned_configs` / `skipped_configs` in every sample meta; validate/finalize check the exact planned sample-id set and per-sample configurations (planned minus skipped); `scripts/audit_tier.py` audits a whole tier on the Hub against the plan (sha256 vs local record, sample ids, configurations, index rows) and `confirm_replaced.py` proves re-uploads replaced their previous versions | two tier-S incidents (partial finalize, truncated final) | v1.0 |
| 2026-09-17 | Tier-S audit script: source regeneration is serialised under a lock — the synthetic generators drive NLMpy through NumPy's global RNG (`seeded_global_rng`), so regenerating landscapes in the audit's download threads produced wrong landscapes and false discrepancies. Not an issue for prepare (one process per shard), the sync validator (single-threaded) or the data loader (process workers) | audit correctness | v1.0 |
| 2026-09-17 | **External review addendum adopted** (`docs/REVIEW_ADDENDUM_2026-09.md`) with three owner corrections recorded at its top (WP1 premise: the block rule was chosen by rel-L2 and max difference against block 1 — measured values in `docs/t4_fidelity.md`; §4 ICE policy resolved; WP8 motivation adds the many-query uses of T1–T3). Order: WP3 → WP5 (design + cost) → WP1 → WP2 on CPU alongside generation, stopping after WP1 and WP2; WP4/6/7 after the GPU allocation; cost estimate before each package; auxiliary artefacts only, v1.0 unchanged | owner | addendum |
| 2026-09-18 | **Owner (WP1 decision):** production T4 targets stay (block-centred Omniscape, block = largest odd ≤ r/10). The block-1 reference subsets become the **official T4 evaluation surface at M and L**: M reference grown to ≈ 1 000 samples covering test_id and every OOD split (real M and ood_region added once generated), L reference kept at 45–60 as a sanity set; T4 leaderboard metrics at M and L are reported against the exact (block-1) targets on these subsets, block-centred-target metrics secondary; per-sample deviations published in the aux index with the > 5 % tail flagged | WP1 measurement (3.1 % mean rel-L2 at M, tail to 25 %) | addendum |
| 2026-09-18 | ICE changed the account's default QoS to `coe-ice` (unusable): `sbatch` without an explicit `--qos coc-ice` is now rejected ("Invalid qos specification"). `configs/cluster/ice.yaml` sets `qos: coc-ice`; the driver already passed it explicitly. Two aux submissions failed silently before the fix and were resubmitted | environment change | v1.0 |
| 2026-09-19 | Supervisors are single-instance across login nodes via `logs/lease_<name>.json` (host, pid, owner, ts; 25-min freshness); pid files are host-local and must not be used to decide liveness from another node. Incident (d) in the generation log | duplicate driver/sync after a session restart on another login node | v1.0 |
| 2026-09-19 | **Owner:** WP2 accepted; L block-1 reference + L block rows (≈ 210 core-h, as L production allows) and the WP5 probe set (≈ 60 core-h) approved; the gnr-2 session exited — the lease-aware L sync loop it restarted there is adopted as is (no upload downtime), the idle S/M loops were stopped (tiers complete and audited); CI fixed (unused locals, lambdas) and commits touching only `docs/status`, `docs/tables`, `docs/figures`, `DECISIONS.md`, `CHANGELOG.md` skip CI (`paths-ignore`) | owner | addendum / v1.0 |
| 2026-09-19 | Walltimes are set from the measured per-landscape tail (shard × p99 × 1.15 + 20 min), not the median: L 8 h / XL 10 h / XXL 10 h; "solved" means the solver completion marker; timed-out shards are re-queued by the driver at the next cycle regardless of running arrays, under the concurrency cap; L uses 1 core (single-threaded solver) | incident (f) | v1.0 |
| 2026-09-21 | Tier audit: an absent task-group file (e.g. `T1R`) is a discrepancy only if a planned configuration of that group survives `skipped_configs` for some sample of the shard; a shard whose every `regions` sample was skipped legitimately has no T1R part (`absent_groups_all_skipped`). The L audit had flagged 41 such shards; no data changed | false positive would otherwise block every tier with small shards; skips are labelled, not missing | generation log (g2) |
| 2026-09-21 | **XL held before submission:** the runbook's XL/XXL core-hour estimates were single-core hours while the profile allocates 4/8 cpus (charged by Slurm), so XL + XXL cannot fit the 13 000 budget as planned (2 727 core-hours left after L); the driver was stopped, a 4-shard XL probe (1 cpu vs 4 cpus) measures the real cost; owner decision requested on the options in `docs/status/latest.md` | budget gate | status |
| 2026-09-21 | **Owner:** cost is not a constraint on ICE — optimise for wall-clock. Full counts (XL 4 000, XXL 400 + 6 strict); the core-hour gate for this run is raised to **30 000**. cpus per task from the XL probe (4 if the speed-up is ≥ 1.5×, else 1), memory to fit (XXL ≈ 15 GB); 4–6 parallel uploaders with per-worker leases and the same verify-then-delete rule (measure GB/h before/after); waves sized so solve output ≈ upload rate, scratch < 200 GB, ≤ 500 queued jobs; XL submitted as soon as the probe result is in | owner | status 2026-09-21 |
| 2026-09-21 | **XL/XXL at 1 cpu per task** (memory-only allocation 16 GB, XXL test shards 20 GB): the XL probe (shards 0-1 at 1 cpu, 2-3 at 4 cpus; 17 landscapes) shows no speed-up (mean per landscape ≈ 2 400 s vs ≈ 2 500 s; Omniscape/CHOLMOD is 95 % of the time and gains nothing from BLAS threads at 1024²), so the owner's rule (4 cpus only if ≥ 1.5×) selects 1 cpu; XL walltime 10 h holds (6 × 4 000 s × 1.15 + 20 min ≈ 8 h) | probe 5880731/5880732 | status |
| 2026-09-21 | `regions` (T1R) problems on which CHOLMOD raises `PosDefException` (high contrast, region-merged nodes; 3.5 % of XL regions rows) are kept as CG+AMG fallback solves at the reference tolerance (`solver = cg+amg` per row, no data change); memory sized for the fallback on resubmission (XL 48 GB, XXL 176 GB); a CHOLMOD-stable regions path is a post-run solver task | diagnostic 5885708 | generation log (h) |
| 2026-09-21 | **Post-run re-solve pass (planned, owner approval pending):** every `solver = cg+amg` row (all `regions`/T1R; ≈ 180 rows over S–XXL) is re-solved before the v1.0 tag with CHOLMOD on the reduced system + one refinement step, true residual verified ≤ 1e-9 per pair, replaced in place on the Hub with provenance (`solver_original = cg+amg`, `resolved_post_run`), index republished, full audits re-run, per-tier counts in the datasheet; ≈ 35 core-h — `docs/post_run_resolve_plan.md` | diagnostic 5887428: direct solve reaches 1e-9 with refinement, CG alone floors at 1e-9–1e-7 | plan |
| 2026-09-21 | **Owner: post-run precision pass approved, two parts before the v1.0 tag** — (1) all ≈ 180 `cg+amg` T1R rows re-solved (reduced-system CHOLMOD + one refinement, true residual ≤ 1e-9 per pair verified, currents/Reff recomputed, in-place rewrite, `solver_original` + `resolved_post_run` kept); (2) every reference row above 1e-9 or unmeasured, target ≤ 1e-9, achieved residual recorded where double precision floors higher, rows that cannot reach 1e-6 QC-flagged and excluded from splits; tier by tier with a full audit after each, index republished once at the end, per-pair residual stored for every T1R row; starts after XXL, must not slow generation. Scope reading (a) vs (b) in `docs/post_run_resolve_plan.md` §6.1 — default (a), owner asked to confirm | owner | plan §6 |
| 2026-09-22 | **Circuitscape solve rescue:** AmpScapeSolve replaces Circuitscape 5.17.1's `solve_linear_system` methods so that a solve whose relative residual is ≥ 1e-4 (where Circuitscape throws and aborts the map) is refined (CHOLMOD: iterative refinement; CG: CHOLMOD factorisation + refinement) instead; bit-identical results whenever Circuitscape's check passes; rescued solves counted per row (`solver_params.rescued_solves`); T4/T3 rows that had failed before the change are repaired by the precision pass at XXL | incident (i): contrast-10⁶ 2048² landscapes failed Circuitscape's internal check in both solvers | generation log (i) |
| 2026-09-23 | **Precision pass complete (S–XXL):** 129 722 rows re-solved in place with provenance; 20 rows above 1e-6 flagged and excluded from splits; five clean audits; indexes republished once — v1.0 data considered final for the tag pending owner review | owner instruction 2026-09-21 | `docs/tables/precision_pass.md`, `docs/post_run_resolve_plan.md` §8 |
| 2026-09-23 | **v1.0 tagged** — GitHub tag `v1.0` (this commit) and Hub revision `v1.0` on `Xirro/AmpScape` (final card, validated Croissant, indexes and split lists after the precision pass, statistics, post-mortem); `CITATION.cff` version 1.0.0, released 2026-09-23; everything after this is GPU work (WP4/WP6/WP7, baselines) | owner 2026-09-23 | card, `docs/generation_postmortem.md` |
| 2026-09-24 | **Subset `lite` (1.0.1, metadata only):** shard prefixes S 40 / M 24 / L 40 (8 000 + 2 400 + 800 landscapes, ≈ 26 GB — sized to the ~25 GB target rather than the example counts, which give 16 GB); nested mini ⊂ lite ⊂ core ⊂ full; index column, split lists and Croissant FileSet; the prefix subsets are synthetic-only by construction (real tiles and `ood_region` only in `full`) — stated on the card | owner | card, CHANGELOG |
| 2026-09-24 | **Owner: Phase 10-full runs on ICE**, no external GPU allocation, the 20 GPU-hour gate is lifted for this phase; order: U-Net and FNO on T1/T4 across tiers (seed 1) first, then the rest, GNN last; WP4 after the headline S runs; WP7 after the best T4 model exists; weekly reports and on schedule changes. Pool: `coc-gpu` L40S, ≤ 10 concurrent jobs, 16-h walltime with self re-queue (`train.py --resume --pause-exit`); ≈ 980 GPU-hours, ≈ 6 calendar days — `docs/phase10_full_schedule.md` | owner | schedule |
| 2026-09-24 | **Owner:** scratch deletion approved — `data/tiles/v1.0/{tiles,resistance,quicklooks}` (≈ 37 GB of tile rasters; regenerable from `data/sources` with `scripts/extract_tiles.py` + `scripts/build_resistance.py`; the 20 WP7 tiles keep their copies under `aux/wp7/tiles`). The metadata parquets and specs under `data/tiles/v1.0` (≈ 30 MB, read by `scripts/dataset_stats_figures.py`) are kept. `data/sources` stays. Purpose: lower the scratch floor so both L groups fit the training cache. |
| 2026-09-24 | **Owner:** finding recorded, no change — in-distribution T4 at S is near-solved by U-Net (rel-L2 0.040 on test_id); the benchmark's difficulty is in T1, in scale transfer (T1 rel-L2 S 0.11 → M 0.24 → L 0.39 for U-Net) and on the published tiles. |
| 2026-09-24 | ViT T1 at L (rel-L2 0.64 test_id / 1.54 test_ood) checked once, no tuning: training did not diverge (train 0.65 → 0.24, val 0.58 → 0.26, monotone; val at M 0.22), the official config is fixed across tiers (patch 4, dim 192, depth 6, learned 32×32 positional embedding bilinearly interpolated to 128×128 at L, global attention over 16,384 tokens, batch 4). The degradation is structural (resolution-inappropriate config), not a training failure; the number stands in the table with this note. |
| 2026-09-24 | **XL/XXL baseline rows come from scale transfer of the L-trained models, not from training at XL** (card: held-out scale for models trained ≤ L; XL has 228 train / 0 val landscapes, XXL none). Nine XL training jobs cancelled and set aside (`runs/abandoned_xl_training/`); 42 transfer-evaluation jobs added (`docs/phase10_full_schedule.md` §5). Harness metrics parallelised (spawned processes) after single-process XL metrics exceeded a 2-h leg. |
| 2026-09-25 | **Check 1 (owner):** Omniscape geometry verified on every v1.0 XL and XXL T4 row from the stored solver parameters — XL block 11 / radius 128 (4,000 rows), XXL block 25 / radius 256 (400 rows), L block 5 / radius 64; CHOLMOD, artifact correction on; block ≤ radius/10 holds, card statement correct. |
| 2026-09-25 | **Check 2 (owner):** amendment C3 (25 % of XL train/val) acted only on real tiles: synthetic XL landscapes have no macro-cell id, the block-hash test used the literal `None` and moved all 2,165 synthetic train/val landscapes to test_id (XL train 228, val 0). Metadata-only fix (1.0.2: per-seed-family share for synthetic XL, val cells among the kept real cells) proposed in `docs/status/latest.md`; not applied pending owner approval. |
| 2026-09-25 | **Check 3 (owner):** scale-aware target variant `--target-norm scale` (T1/T3 k = √(N_valid/512²), T4 k = 64/r_tier; `ampscape.models.common.target_scale`), one run per model at L (seed 1), same zero-shot XL/XXL transfer, no calibration on XL/XXL; zero-shot rows of the official configs stay as measured. Estimated ≈ 30 GPU-h (+ 15–35 for the GNN). |
| 2026-09-25 | Incident: `scripts/transfer_eval.py` was truncated to an empty file by a failed write at the scratch quota and committed empty (e59af86, 09:40Z); restored from 128870c with the later changes at 12:05Z. Transfer jobs submitted in between ran an empty script and were resubmitted by the driver (no results lost). |
| 2026-09-26 | **v1.0.2 (owner-approved, metadata only):** XL C3 corrected (train 880 / val 131 / test_id 2,335), split lists rebuilt as cross-tier unions (the v1.0 lists held one tier — found during the None-key audit; only the C3 hash had a None key: the block-order and seed-family hashes and the strict XXL check use defined keys), transfer rows re-aggregated, Croissant regenerated, tag v1.0.2 on GitHub and the Hub. |
